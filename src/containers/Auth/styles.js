import styled from "styled-components";
import { Column } from "components/shared/styles";
export const StyledAuth = styled.div`
  display: grid;
  place-items: center;
`;

export const StyledLogin = styled(Column)`
  align-items: center;
`;
export const StyledSignup = styled(Column)`
  align-items: center;
`;
